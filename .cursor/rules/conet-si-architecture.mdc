---
description: CoNET-SI node architecture - pure JS, no nginx
globs: src/CoNET-SI/**/*
alwaysApply: false
---

# CoNET-SI Node Architecture

**CoNET-SI 节点程序没有部署 nginx，完全由 JavaScript/Node.js 主导一切。**

- HTTP/HTTPS 请求由 `server.ts` 直接处理（`createServer` / `createServerSSL`）
- 路由、代理、响应头等逻辑均在 JS 中实现
- 不要假设存在 nginx 反向代理；排查问题时以 CoNET-SI 自身逻辑为准
