---
description: Beamio BUnit fee standard for OpenContainer merchant payments and related flows
globs: "**/MemberCard.ts"
alwaysApply: false
---

# Beamio BUnit Fee Protocol

**适用范围**：OpenContainer 商户收款、及需按 Beamio 收费标准计费的场景。

## 汇率与费率

- **1 USDC = 100 BUnit**（1 BUnit = 0.01 USDC）
- **费率**：金额的 **0.8%**
- **最低**：2 BUnit
- **最高**：200 BUnit
- **≥5000 USDC**：固定 500 BUnit

## 计算逻辑

1. 将 currency 按当前汇率换算为 USDC（amountUSDC6）
2. 基础费 = amountUSDC6 × 0.8%
3. 费额 BUnit = 基础费 USDC × 100
4. 钳位：min(2, max(200, 费额))；若 amountUSDC6 ≥ 5_000_000_000 则固定 500 BUnit

## 承担方

- **OpenContainer 商户收款**：BUnit 由**收款方（payee）**负担
- 其他场景（如 x402、Container）按各自约定

## 记账字段

- `bServiceUSDC6`：B 服务费折算 USDC（6 位精度）
- `bServiceUnits6`：B 服务费单位数（6 位精度）
- `feePayer`：费用承担方地址（EOA）

## 实现参考

`calcBeamioBUnitFee(amountUSDC6)` 已封装于 MemberCard.ts，供 OpenContainer 等调用。
