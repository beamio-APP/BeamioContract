---
description: CoNET zero-trust relay protocol
alwaysApply: true
---

# CoNET Zero-Trust Node Protocol

CoNET 节点通信遵循 **A 跳板 / B 解密** 的零信任协议，代码实现必须满足以下约束：

## Core Model

- `A`（入口/跳板节点）可以看到客户端来源 IP 与最小路由元数据，但**不能**解密业务明文。
- `B`（目标路由节点）持有对应私钥，负责解密并处理业务内容。
- 任意中间节点都必须被视为不可信，系统安全依赖端到端加密而非中间层信任。

## Implementation Requirements

- 所有 gossip/post 业务 payload 必须先用目标路由公钥加密，再发送到对应路由节点。
- 严禁将“为 B 加密”的 payload 随机发送到不匹配的节点。
- 浏览器/PWA 场景仅使用 HTTPS/WSS，不回退到 HTTP。
- 健康探测仅用于可达性判断，不得把探测成功等同于可解密或可读明文。

## Security Red Lines

- 不得在日志中输出私钥、会话密钥、完整明文消息、完整 PGP 密文。
- 不得新增任何绕过加密的调试后门（例如明文镜像、测试回显明文）。
- 不得在中间层节点新增“尝试解密”逻辑。

## Review Checklist

- 是否先做“路由公钥 -> 目标节点”匹配，再建立连接？
- 是否保证中间节点只获得必要元数据（如 IP），而无法获知通讯内容？
- 是否保持零信任假设（任何单点泄露不应暴露消息明文）？
