---
description: SilentPassUI gossip route-match and health gate
globs: src/SilentPassUI/src/services/chat.ts
alwaysApply: false
---

# SilentPassUI Gossip Connection Guardrails

本规则仅适用于 `SilentPassUI` 的 gossip 客户端连接逻辑。

## Must Follow Order

1. 先通过 `router armored public key` 从节点列表中筛选目标路由节点。
2. 仅对“路由匹配节点”执行 HTTPS 健康探测。
3. 仅当健康节点集合非空时，才允许调用 `startGossip`。
4. 空集合时必须中止并记录错误，不得回退到全量随机节点。

## Mandatory Constraints

- 禁止“加密给路由 B，却随机连接其他节点”。
- 禁止健康探测失败后回退到 `nodes` 全集合。
- 连接失败标记 bad、连接成功标记 healthy，保持健康缓存更新。
- 浏览器/PWA 仅使用 `https://{domain}.conet.network/post` 建连。

## Code Review Checks

- `connectToGossipNode` 内是否存在 route 节点筛选步骤？
- `startGossip` 的输入是否来自 healthy route nodes？
- `healthyNodes.length === 0` 时是否直接 return？
